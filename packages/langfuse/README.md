# Langfuse

Langfuse is a self-hosted LLM observability platform. This package runs web, worker, PostgreSQL, Redis, ClickHouse, and MinIO with loopback-only web exposure.

Replace all default secrets before real use.
