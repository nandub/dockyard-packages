apiVersion: dockyard.dev/v1alpha1
name: qdrant
version: 0.1.0
description: Vector database for embeddings and retrieval-augmented generation workloads.
compose:
  base: compose.yaml
  overlays: {}
values:
  default: values.yaml
schema: values.schema.json

