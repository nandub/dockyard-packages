apiVersion: dockyard.dev/v1alpha1
name: n8n
version: 0.1.0
description: Workflow automation platform with native AI capabilities and persistent workflow data.
compose:
  base: compose.yaml
  overlays: {}
values:
  default: values.yaml
  schema: values.schema.json
