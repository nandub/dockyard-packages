apiVersion: dockyard.dev/v1alpha1
name: redis
version: 0.2.0
description: Redis-compatible in-memory data store with authentication, persistence, health checks, and resource controls.
compose:
  base: compose.yaml
  overlays: {}
values:
  default: values.yaml
schema: values.schema.json
