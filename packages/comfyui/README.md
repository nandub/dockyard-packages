# ComfyUI

ComfyUI is a node-based workflow UI for visual AI generation. Defaults bind to `127.0.0.1:8188` and use a persistent workspace volume.

This package uses a community Docker image; review and pin it before production use.
