{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": [
    "image",
    "defaultServer",
    "service"
  ],
  "properties": {
    "image": {
      "type": "object",
      "required": [
        "repository",
        "tag"
      ],
      "properties": {
        "repository": {
          "type": "string",
          "minLength": 1,
          "description": "Adminer image repository."
        },
        "tag": {
          "type": "string",
          "minLength": 1,
          "description": "Adminer image tag."
        }
      },
      "additionalProperties": false,
      "description": "Container image selection."
    },
    "defaultServer": {
      "type": "string",
      "minLength": 1,
      "description": "Default database server hostname shown by Adminer."
    },
    "service": {
      "type": "object",
      "required": [
        "port"
      ],
      "properties": {
        "port": {
          "type": "integer",
          "minimum": 1,
          "description": "Host port published for Adminer.",
          "maximum": 65535
        }
      },
      "additionalProperties": false,
      "description": "Service exposure."
    }
  },
  "additionalProperties": false
}
