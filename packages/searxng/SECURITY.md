# Security notes

Public SearXNG instances need careful abuse protection and rate limiting. Keep the default loopback binding for private RAG/search use, or expose only through a hardened reverse proxy.
