# Security notes

Flowise can store model provider credentials, prompts, files, and workflow data. Keep the service bound to loopback unless a reverse proxy with TLS and authentication is configured. Change the default password before use outside a local workstation.
